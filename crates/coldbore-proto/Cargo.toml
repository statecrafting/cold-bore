[package]
name = "coldbore-proto"
description = "Shared vocabulary for the cold-bore pipeline: frame, control, metrics and event contracts, broker topology constants, env configuration"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[package.metadata.spec-spine]
spec = "002-telemetry-model"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
